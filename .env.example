# SQL Server Connection Configuration
# Copy this file to .env and update with your actual credentials

# SQL Server connection details
SQL_SERVER=localhost
SQL_PORT=1433
SQL_DATABASE=TicketAnalytics

# Authentication - Choose one method:
# Method 1: Windows Authentication (leave USERNAME and PASSWORD empty)
SQL_USERNAME=
SQL_PASSWORD=

# Method 2: SQL Server Authentication (provide username and password)
# SQL_USERNAME=your_username
# SQL_PASSWORD=your_password

# Driver (ensure this driver is installed on your system)
# Common options:
# - ODBC Driver 17 for SQL Server
# - ODBC Driver 18 for SQL Server
# - SQL Server Native Client 11.0
SQL_DRIVER=ODBC Driver 17 for SQL Server

# Connection settings
SQL_CONNECTION_TIMEOUT=30
SQL_COMMAND_TIMEOUT=60

# Table name
SQL_TICKETS_TABLE=tickets

# ============================================================
# Auth0 Authentication Configuration
# ============================================================

# Auth0 Application Settings (get from Auth0 Dashboard)
# Format: your-tenant.auth0.com or your-tenant.us.auth0.com
AUTH0_DOMAIN=your-tenant.auth0.com
AUTH0_CLIENT_ID=your_client_id_here
AUTH0_CLIENT_SECRET=your_client_secret_here

# Callback URL - MUST match Auth0 application settings
# For local development: http://localhost:8501
# For production: https://your-domain.com
AUTH0_CALLBACK_URL=http://localhost:8501

# OAuth Scopes (space-separated)
# openid profile email are recommended defaults
AUTH0_SCOPE=openid profile email

# Optional: Auth0 API Audience (only needed if calling Auth0 Management API)
# AUTH0_AUDIENCE=

# Session lifetime in seconds (default: 3600 = 1 hour)
AUTH0_SESSION_LIFETIME=3600

# Users table name in SQL Server
AUTH0_USERS_TABLE=users

# ============================================================
# SECURITY NOTES FOR AUTH0:
# 1. Keep CLIENT_SECRET secure - NEVER commit to version control
# 2. Callback URL in .env MUST EXACTLY match Auth0 dashboard settings
# 3. For production, use HTTPS for callback URLs
# 4. Regularly rotate CLIENT_SECRET in Auth0 dashboard
# ============================================================

# ============================================================
# IMPORTANT SECURITY NOTES:
# 1. NEVER commit the .env file to version control
# 2. Add .env to your .gitignore file
# 3. Keep your credentials secure
# 4. Use Windows Authentication when possible for better security
# ============================================================
